rootProject.name = 'library'

include(
        "messages",
        "serialize-test-fixtures",
        "serialize-java",
        "serialize-proto",
        "serialize-kryo",
        "analysis",
        )
